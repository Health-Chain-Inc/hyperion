CREATE TABLE IF NOT EXISTS `schema_history` (
    `id` BIGINT AUTO_INCREMENT,
    `table_name` VARCHAR(255) NOT NULL,
    `created_date` datetime NOT NULL,
    `created_by` VARCHAR(100) NOT NULL,
    `updated_date` datetime,
    `version` INT NOT NULL,
    `status` VARCHAR(50) NOT NULL,
    `description` TEXT
) PRIMARY KEY (`id`) PROPERTIES (
    "compression" = "LZ4",
    "datacache.enable" = "true",
    "enable_async_write_back" = "false",
    "enable_persistent_index" = "true",
    "persistent_index_type" = "CLOUD_NATIVE",
    "replication_num" = "1"
);

CREATE TABLE IF NOT EXISTS `pipeline_meta_info` (
    `id` BIGINT AUTO_INCREMENT,
    `property` VARCHAR(255) NOT NULL,
    `description` TEXT,
    `status` VARCHAR(50) NOT NULL,
    `created_date` datetime NOT NULL,
    `created_by` VARCHAR(100) NOT NULL,
    `updated_date` datetime
) PRIMARY KEY (`id`) PROPERTIES (
    "compression" = "LZ4",
    "datacache.enable" = "true",
    "enable_async_write_back" = "false",
    "enable_persistent_index" = "true",
    "persistent_index_type" = "CLOUD_NATIVE",
    "replication_num" = "1"
);

CREATE TABLE IF NOT EXISTS  `file_export_logger` (
  `uuid` varchar(36) NOT NULL COMMENT "",
  `source_file_name` varchar(250) NOT NULL COMMENT "",
  `file_move_status` boolean NULL COMMENT ""
) ENGINE=OLAP
PRIMARY KEY(`uuid`)
COMMENT "OLAP"
DISTRIBUTED BY HASH(`uuid`)
PROPERTIES (
"compression" = "LZ4",
"datacache.enable" = "true",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num" = "1"
);

CREATE TABLE IF NOT EXISTS `dollar_export_logger` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT "",
  `since_date_time` datetime NOT NULL COMMENT "",
  `till_date_time` datetime NOT NULL COMMENT "",
  `resource_type` varchar(50) NOT NULL COMMENT "",
  `status_url` varchar(500) NULL COMMENT "",
  `dollar_export_status` varchar(20),
  `inserted_date` datetime NULL DEFAULT CURRENT_TIMESTAMP COMMENT "") ENGINE=OLAP
PRIMARY KEY(`id`)
COMMENT "OLAP"
DISTRIBUTED BY HASH(`id`)
PROPERTIES (
"compression" = "LZ4",
"datacache.enable" = "true",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num" = "1"
);

CREATE TABLE IF NOT EXISTS `codeableconcept` (
    `id` varchar(100) NOT NULL COMMENT "",
    `field_name` varchar(255) NOT NULL COMMENT "",
    `seq_no` bigint(20) NOT NULL COMMENT "",
    `system` varchar(255) NULL COMMENT "",
    `version` varchar(255) NULL COMMENT "",
    `code` varchar(255) NULL COMMENT "",
    `display` varchar(255) NULL COMMENT "",
    `userselected` boolean COMMENT "",
    `text` varchar(1048576) NULL COMMENT "",
    `extension` array<json> NULL COMMENT "",
    `updated_date` datetime NULL DEFAULT CURRENT_TIMESTAMP COMMENT "",
    INDEX codeableconcept_fieldname_index (`field_name`) USING BITMAP COMMENT '',
    INDEX codeableconcept_code_index (`code`) USING BITMAP COMMENT ''
) ENGINE = OLAP PRIMARY KEY (`id`, `field_name`, `seq_no`) COMMENT "OLAP" DISTRIBUTED BY HASH (`id`) BUCKETS 16 PROPERTIES (
    "bloom_filter_columns" = "id, system",
    "compression" = "LZ4",
    "datacache.enable" = "true",
    "enable_async_write_back" = "false",
    "enable_persistent_index" = "true",
    "persistent_index_type" = "CLOUD_NATIVE",
    "replication_num" = "1",
    "colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `reference` (
    `id` varchar(100) NOT NULL COMMENT "",
    `field_name` varchar(255) NOT NULL COMMENT "",
    `seq_no` bigint(20) NOT NULL COMMENT "",
    `reference` varchar(255) NULL COMMENT "",
    `type` varchar(255) NULL COMMENT "",
    `identifier` json NULL COMMENT "",
    `display` varchar(255) NULL COMMENT "",
    `extension` array<json> NULL COMMENT "",
    `updated_date` datetime NULL DEFAULT CURRENT_TIMESTAMP COMMENT "",
    INDEX reference_fieldname_index (`field_name`) USING BITMAP COMMENT ''
) ENGINE = OLAP PRIMARY KEY (`id`, `field_name`, `seq_no`) COMMENT "OLAP" DISTRIBUTED BY HASH (`id`) BUCKETS 16 PROPERTIES (
    "bloom_filter_columns" = "id, reference",
    "compression" = "LZ4",
    "datacache.enable" = "true",
    "enable_async_write_back" = "false",
    "enable_persistent_index" = "true",
    "persistent_index_type" = "CLOUD_NATIVE",
    "replication_num" = "1",
    "colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `identifier` (
    `id` varchar(100) NOT NULL COMMENT "",
    `field_name` varchar(255) NOT NULL COMMENT "",
    `seq_no` bigint(20) NOT NULL COMMENT "",
    `use` varchar(255) NULL COMMENT "",
    `type` json NULL COMMENT "",
    `system` varchar(255) NULL COMMENT "",
    `value` varchar(255) NULL COMMENT "",
    `period_start` datetime NULL COMMENT "",
    `period_end` datetime NULL COMMENT "",
    `assigner` json NULL COMMENT "",
    `extension` array<json> NULL COMMENT "",
    `updated_date` datetime NULL DEFAULT CURRENT_TIMESTAMP COMMENT "",
    INDEX identifier_fieldname_index (`field_name`) USING BITMAP COMMENT ''
) ENGINE = OLAP PRIMARY KEY (`id`, `field_name`, `seq_no`) COMMENT "OLAP" DISTRIBUTED BY HASH (`id`) BUCKETS 16 PROPERTIES (
    "bloom_filter_columns" = "id, system",
    "compression" = "LZ4",
    "datacache.enable" = "true",
    "enable_async_write_back" = "false",
    "enable_persistent_index" = "true",
    "persistent_index_type" = "CLOUD_NATIVE",
    "replication_num" = "1",
    "colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `fhir_lineage` (
  `filepath_id` varchar(36) NOT NULL COMMENT "",
  `resource_type` varchar(30) NOT NULL COMMENT "",
  `fhir_request_url` varchar(500) NULL COMMENT "",
  `record_count` int(11) NULL COMMENT "",
  `pipeline_type` varchar(20) NULL COMMENT "",
  `destination_location` varchar(500) NULL COMMENT "",
  `is_inserted` boolean NULL COMMENT "",
  `retry_count` int(11) NULL COMMENT "",
  `error_code` varchar(3) NULL COMMENT "",
  `reject_location` varchar(500) NULL COMMENT "",
  `created_date` datetime NULL DEFAULT CURRENT_TIMESTAMP COMMENT "",
  `updated_date` datetime NULL COMMENT ""
) ENGINE=OLAP
PRIMARY KEY(`filepath_id`)
COMMENT "OLAP"
DISTRIBUTED BY HASH(`filepath_id`) BUCKETS 8
PROPERTIES (
"compression" = "LZ4",
"datacache.enable" = "true",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num" = "1",
"colocate_with" = "audit_group"
);


CREATE TABLE IF NOT EXISTS `fhir_audit` (
  `resource_id` varchar(100) NOT NULL COMMENT "",
  `resource_type` varchar(30) NOT NULL COMMENT "",
  `meta_versionid` int(11) NULL COMMENT "",
  `meta_lastupdated` datetime NOT NULL COMMENT "",
  `meta_source` varchar(20) NULL COMMENT "",
  `operation` varchar(20) NULL COMMENT "",
  `pipeline_type` varchar(20) NULL COMMENT "",
  `filepath_id` varchar(36) NULL COMMENT "",
  `created_date` datetime NULL DEFAULT CURRENT_TIMESTAMP COMMENT ""
) ENGINE=OLAP
DUPLICATE KEY(`resource_id`)
COMMENT "OLAP"
PARTITION BY date_trunc('day', meta_lastupdated)
DISTRIBUTED BY HASH(`filepath_id`) BUCKETS 8
ORDER BY(resource_id, meta_versionid)
PROPERTIES (
"bloom_filter_columns" = "resource_id, filepath_id",
"compression" = "LZ4",
"datacache.enable" = "true",
"enable_async_write_back" = "false",
"replication_num" = "1",
"partition_live_number" = "365",
"colocate_with" = "audit_group"
);

CREATE TABLE IF NOT EXISTS `fhir_export_logger` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT "",
  `since_date_time` datetime NOT NULL COMMENT "",
  `till_date_time` datetime NOT NULL COMMENT "",
  `created_date` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT ""
) ENGINE=OLAP
PRIMARY KEY(`id`)
COMMENT "OLAP"
DISTRIBUTED BY HASH(`id`)
PROPERTIES (
"compression" = "LZ4",
"datacache.enable" = "true",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num" = "1"
);

 CREATE TABLE IF NOT EXISTS `metadata_backup_log` (
    `backup_id` VARCHAR(36) NOT NULL,
    `hostname` VARCHAR(256) NOT NULL,
    `backup_timestamp` DATETIME NOT NULL,
    `status` VARCHAR(20) NOT NULL,
    `folder_name` VARCHAR(256) NULL,
    `total_files` INT NULL,
    `total_size_bytes` BIGINT,
    `verified_files` INT NULL,
    `attempts` INT NULL,
    `error_message` VARCHAR(2048) NULL,
    `duration_seconds` DOUBLE NULL
  ) ENGINE = OLAP
PRIMARY KEY(`backup_id`)
COMMENT "OLAP"
DISTRIBUTED BY HASH(`backup_id`) BUCKETS 1
PROPERTIES (
"compression" = "LZ4",
"datacache.enable" = "true",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num" = "1"
);


CREATE TABLE IF NOT EXISTS `account` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `name` STRING,
    `serviceperiod_extension` ARRAY<JSON>,
    `serviceperiod_start` DATETIME,
    `serviceperiod_end` DATETIME,
    `coverage` ARRAY<JSON>,
    `description` STRING,
    `guarantor` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX account_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name, description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `activitydefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `subtitle` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `usage` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `library` ARRAY<STRING>,
    `kind` STRING,
    `profile` STRING,
    `intent` STRING,
    `priority` STRING,
    `donotperform` BOOLEAN,
    `timingtiming_extension` ARRAY<JSON>,
    `timingtiming_event` ARRAY<STRING>,
    `timingtiming_repeat` JSON,
    `timingtiming_code` JSON,
    `timingdatetime` STRING,
    `timingage_extension` ARRAY<JSON>,
    `timingage_value` DECIMAL(20,10),
    `timingage_comparator` STRING,
    `timingage_unit` STRING,
    `timingage_system` STRING,
    `timingage_code` STRING,
    `timingperiod_extension` ARRAY<JSON>,
    `timingperiod_start` DATETIME,
    `timingperiod_end` DATETIME,
    `timingrange_extension` ARRAY<JSON>,
    `timingrange_low` JSON,
    `timingrange_high` JSON,
    `timingduration_extension` ARRAY<JSON>,
    `timingduration_value` DECIMAL(20,10),
    `timingduration_comparator` STRING,
    `timingduration_unit` STRING,
    `timingduration_system` STRING,
    `timingduration_code` STRING,
    `participant` ARRAY<JSON>,
    `quantity_extension` ARRAY<JSON>,
    `quantity_value` DECIMAL(20,10),
    `quantity_comparator` STRING,
    `quantity_unit` STRING,
    `quantity_system` STRING,
    `quantity_code` STRING,
    `dosage_extension` ARRAY<ARRAY<JSON>>,
    `dosage_sequence` ARRAY<INTEGER>,
    `dosage_text` ARRAY<STRING>,
    `dosage_additionalinstruction` ARRAY<ARRAY<JSON>>,
    `dosage_patientinstruction` ARRAY<STRING>,
    `dosage_timing` ARRAY<JSON>,
    `dosage_asneededboolean` ARRAY<BOOLEAN>,
    `dosage_asneededcodeableconcept` ARRAY<JSON>,
    `dosage_site` ARRAY<JSON>,
    `dosage_route` ARRAY<JSON>,
    `dosage_method` ARRAY<JSON>,
    `dosage_doseandrate` ARRAY<ARRAY<JSON>>,
    `dosage_maxdoseperperiod` ARRAY<JSON>,
    `dosage_maxdoseperadministration` ARRAY<JSON>,
    `dosage_maxdoseperlifetime` ARRAY<JSON>,
    `transform` STRING,
    `dynamicvalue` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX activitydefinition_status_index (`status`) USING BITMAP,
    INDEX activitydefinition_experimental_index (`experimental`) USING BITMAP,
    INDEX activitydefinition_kind_index (`kind`) USING BITMAP,
    INDEX activitydefinition_donotperform_index (`donotperform`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `adverseevent` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `actuality` STRING,
    `date` DATETIME,
    `detected` DATETIME,
    `recordeddate` DATETIME,
    `suspectentity` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX adverseevent_actuality_index (`actuality`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `allergyintolerance` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `type` STRING,
    `category` ARRAY<STRING>,
    `criticality` STRING,
    `onsetdatetime` STRING,
    `onsetage_extension` ARRAY<JSON>,
    `onsetage_value` DECIMAL(20,10),
    `onsetage_comparator` STRING,
    `onsetage_unit` STRING,
    `onsetage_system` STRING,
    `onsetage_code` STRING,
    `onsetperiod_extension` ARRAY<JSON>,
    `onsetperiod_start` DATETIME,
    `onsetperiod_end` DATETIME,
    `onsetrange_extension` ARRAY<JSON>,
    `onsetrange_low` JSON,
    `onsetrange_high` JSON,
    `onsetstring` STRING,
    `recordeddate` DATETIME,
    `lastoccurrence` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `reaction` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX allergyintolerance_type_index (`type`) USING BITMAP,
    INDEX allergyintolerance_criticality_index (`criticality`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `appointment` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `priority` INTEGER,
    `description` STRING,
    `start` STRING,
    `end` STRING,
    `minutesduration` INTEGER,
    `created` DATETIME,
    `comment` STRING,
    `patientinstruction` STRING,
    `participant` ARRAY<JSON>,
    `requestedperiod_extension` ARRAY<ARRAY<JSON>>,
    `requestedperiod_start` ARRAY<STRING>,
    `requestedperiod_end` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX appointment_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `appointmentresponse` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `start` STRING,
    `end` STRING,
    `participantstatus` STRING,
    `comment` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX appointmentresponse_participantstatus_index (`participantstatus`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `auditevent` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `action` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `recorded` STRING,
    `outcome` STRING,
    `outcomedesc` STRING,
    `agent` ARRAY<JSON>,
    `source_site` STRING,
    `source_observer` JSON,
    `source_type` JSON,
    `entity` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX auditevent_action_index (`action`) USING BITMAP,
    INDEX auditevent_outcome_index (`outcome`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "outcomedesc",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `basic` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `created` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `binary` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `contenttype` STRING,
    `data` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX binary_contenttype_index (`contenttype`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `biologicallyderivedproduct` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `productcategory` STRING,
    `status` STRING,
    `quantity` INTEGER,
    `collection_collector` JSON,
    `collection_source` JSON,
    `collection_collecteddatetime` STRING,
    `collection_collectedperiod` JSON,
    `processing` ARRAY<JSON>,
    `manipulation_description` STRING,
    `manipulation_timedatetime` STRING,
    `manipulation_timeperiod` JSON,
    `storage` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `bodystructure` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `description` STRING,
    `image_extension` ARRAY<ARRAY<JSON>>,
    `image_contenttype` ARRAY<STRING>,
    `image_language` ARRAY<STRING>,
    `image_data` ARRAY<STRING>,
    `image_url` ARRAY<STRING>,
    `image_size` ARRAY<INTEGER>,
    `image_hash` ARRAY<STRING>,
    `image_title` ARRAY<STRING>,
    `image_creation` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX bodystructure_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `bundle` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `type` STRING,
    `timestamp` STRING,
    `total` INTEGER,
    `link` ARRAY<JSON>,
    `entry` ARRAY<JSON>,
    `signature_extension` ARRAY<JSON>,
    `signature_type` ARRAY<JSON>,
    `signature_when` STRING,
    `signature_who` JSON,
    `signature_onbehalfof` JSON,
    `signature_targetformat` STRING,
    `signature_sigformat` STRING,
    `signature_data` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX bundle_type_index (`type`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `capabilitystatement` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `kind` STRING,
    `instantiates` ARRAY<STRING>,
    `imports` ARRAY<STRING>,
    `software_name` STRING,
    `software_version` STRING,
    `software_releasedate` DATETIME,
    `implementation_description` STRING,
    `implementation_url` STRING,
    `implementation_custodian` JSON,
    `fhirversion` STRING,
    `format` ARRAY<STRING>,
    `patchformat` ARRAY<STRING>,
    `implementationguide` ARRAY<STRING>,
    `rest` ARRAY<JSON>,
    `messaging` ARRAY<JSON>,
    `document` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX capabilitystatement_status_index (`status`) USING BITMAP,
    INDEX capabilitystatement_experimental_index (`experimental`) USING BITMAP,
    INDEX capabilitystatement_kind_index (`kind`) USING BITMAP,
    INDEX capabilitystatement_fhirversion_index (`fhirversion`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `careplan` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `status` STRING,
    `intent` STRING,
    `title` STRING,
    `description` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `created` DATETIME,
    `activity` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX careplan_status_index (`status`) USING BITMAP,
    INDEX careplan_intent_index (`intent`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "title, description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `careteam` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `name` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `participant` ARRAY<JSON>,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX careteam_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `catalogentry` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `orderable` BOOLEAN,
    `status` STRING,
    `validityperiod_extension` ARRAY<JSON>,
    `validityperiod_start` DATETIME,
    `validityperiod_end` DATETIME,
    `validto` DATETIME,
    `lastupdated` DATETIME,
    `relatedentry` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX catalogentry_orderable_index (`orderable`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `chargeitem` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `definitionuri` ARRAY<STRING>,
    `definitioncanonical` ARRAY<STRING>,
    `status` STRING,
    `occurrencedatetime` STRING,
    `occurrenceperiod_extension` ARRAY<JSON>,
    `occurrenceperiod_start` DATETIME,
    `occurrenceperiod_end` DATETIME,
    `occurrencetiming_extension` ARRAY<JSON>,
    `occurrencetiming_event` ARRAY<STRING>,
    `occurrencetiming_repeat` JSON,
    `occurrencetiming_code` JSON,
    `performer` ARRAY<JSON>,
    `quantity_extension` ARRAY<JSON>,
    `quantity_value` DECIMAL(20,10),
    `quantity_comparator` STRING,
    `quantity_unit` STRING,
    `quantity_system` STRING,
    `quantity_code` STRING,
    `factoroverride` DECIMAL(20,10),
    `priceoverride_extension` ARRAY<JSON>,
    `priceoverride_value` DECIMAL(20,10),
    `priceoverride_currency` STRING,
    `overridereason` STRING,
    `entereddate` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX chargeitem_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `chargeitemdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `title` STRING,
    `derivedfromuri` ARRAY<STRING>,
    `partof` ARRAY<STRING>,
    `replaces` ARRAY<STRING>,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `applicability` ARRAY<JSON>,
    `propertygroup` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX chargeitemdefinition_status_index (`status`) USING BITMAP,
    INDEX chargeitemdefinition_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `claim` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `use` STRING,
    `billableperiod_extension` ARRAY<JSON>,
    `billableperiod_start` DATETIME,
    `billableperiod_end` DATETIME,
    `created` DATETIME,
    `related` ARRAY<JSON>,
    `payee_type` JSON,
    `payee_party` JSON,
    `careteam` ARRAY<JSON>,
    `supportinginfo` ARRAY<JSON>,
    `diagnosis` ARRAY<JSON>,
    `procedure` ARRAY<JSON>,
    `insurance` ARRAY<JSON>,
    `accident_date` DATETIME,
    `accident_type` JSON,
    `accident_locationaddress` JSON,
    `accident_locationreference` JSON,
    `item` ARRAY<JSON>,
    `total_extension` ARRAY<JSON>,
    `total_value` DECIMAL(20,10),
    `total_currency` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX claim_status_index (`status`) USING BITMAP,
    INDEX claim_use_index (`use`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `claimresponse` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `use` STRING,
    `created` DATETIME,
    `outcome` STRING,
    `disposition` STRING,
    `preauthref` STRING,
    `preauthperiod_extension` ARRAY<JSON>,
    `preauthperiod_start` DATETIME,
    `preauthperiod_end` DATETIME,
    `item` ARRAY<JSON>,
    `additem` ARRAY<JSON>,
    `adjudication` ARRAY<JSON>,
    `total` ARRAY<JSON>,
    `payment_type` JSON,
    `payment_adjustment` JSON,
    `payment_adjustmentreason` JSON,
    `payment_date` DATETIME,
    `payment_amount` JSON,
    `payment_identifier` JSON,
    `form_extension` ARRAY<JSON>,
    `form_contenttype` STRING,
    `form_language` STRING,
    `form_data` STRING,
    `form_url` STRING,
    `form_size` INTEGER,
    `form_hash` STRING,
    `form_title` STRING,
    `form_creation` DATETIME,
    `processnote` ARRAY<JSON>,
    `insurance` ARRAY<JSON>,
    `error` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX claimresponse_status_index (`status`) USING BITMAP,
    INDEX claimresponse_use_index (`use`) USING BITMAP,
    INDEX claimresponse_outcome_index (`outcome`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `clinicalimpression` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `description` STRING,
    `effectivedatetime` STRING,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `date` DATETIME,
    `investigation` ARRAY<JSON>,
    `protocol` ARRAY<STRING>,
    `summary` STRING,
    `finding` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX clinicalimpression_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `codesystem` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `casesensitive` BOOLEAN,
    `valueset` STRING,
    `hierarchymeaning` STRING,
    `compositional` BOOLEAN,
    `versionneeded` BOOLEAN,
    `content` STRING,
    `supplements` STRING,
    `count` INTEGER,
    `filter` ARRAY<JSON>,
    `property` ARRAY<JSON>,
    `concept` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX codesystem_status_index (`status`) USING BITMAP,
    INDEX codesystem_experimental_index (`experimental`) USING BITMAP,
    INDEX codesystem_casesensitive_index (`casesensitive`) USING BITMAP,
    INDEX codesystem_hierarchymeaning_index (`hierarchymeaning`) USING BITMAP,
    INDEX codesystem_compositional_index (`compositional`) USING BITMAP,
    INDEX codesystem_versionneeded_index (`versionneeded`) USING BITMAP,
    INDEX codesystem_content_index (`content`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `communication` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `status` STRING,
    `priority` STRING,
    `sent` DATETIME,
    `received` DATETIME,
    `payload` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX communication_status_index (`status`) USING BITMAP,
    INDEX communication_priority_index (`priority`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `communicationrequest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `priority` STRING,
    `donotperform` BOOLEAN,
    `payload` ARRAY<JSON>,
    `occurrencedatetime` STRING,
    `occurrenceperiod_extension` ARRAY<JSON>,
    `occurrenceperiod_start` DATETIME,
    `occurrenceperiod_end` DATETIME,
    `authoredon` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX communicationrequest_status_index (`status`) USING BITMAP,
    INDEX communicationrequest_priority_index (`priority`) USING BITMAP,
    INDEX communicationrequest_donotperform_index (`donotperform`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `compartmentdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `code` STRING,
    `search` BOOLEAN,
    `resource` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX compartmentdefinition_status_index (`status`) USING BITMAP,
    INDEX compartmentdefinition_experimental_index (`experimental`) USING BITMAP,
    INDEX compartmentdefinition_code_index (`code`) USING BITMAP,
    INDEX compartmentdefinition_search_index (`search`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `composition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `date` DATETIME,
    `title` STRING,
    `confidentiality` STRING,
    `attester` ARRAY<JSON>,
    `relatesto` ARRAY<JSON>,
    `event` ARRAY<JSON>,
    `section` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX composition_status_index (`status`) USING BITMAP,
    INDEX composition_confidentiality_index (`confidentiality`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "title",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `conceptmap` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `sourceuri` STRING,
    `sourcecanonical` STRING,
    `targeturi` STRING,
    `targetcanonical` STRING,
    `group` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX conceptmap_status_index (`status`) USING BITMAP,
    INDEX conceptmap_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `condition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `onsetdatetime` STRING,
    `onsetage_extension` ARRAY<JSON>,
    `onsetage_value` DECIMAL(20,10),
    `onsetage_comparator` STRING,
    `onsetage_unit` STRING,
    `onsetage_system` STRING,
    `onsetage_code` STRING,
    `onsetperiod_extension` ARRAY<JSON>,
    `onsetperiod_start` DATETIME,
    `onsetperiod_end` DATETIME,
    `onsetrange_extension` ARRAY<JSON>,
    `onsetrange_low` JSON,
    `onsetrange_high` JSON,
    `onsetstring` STRING,
    `abatementdatetime` STRING,
    `abatementage_extension` ARRAY<JSON>,
    `abatementage_value` DECIMAL(20,10),
    `abatementage_comparator` STRING,
    `abatementage_unit` STRING,
    `abatementage_system` STRING,
    `abatementage_code` STRING,
    `abatementperiod_extension` ARRAY<JSON>,
    `abatementperiod_start` DATETIME,
    `abatementperiod_end` DATETIME,
    `abatementrange_extension` ARRAY<JSON>,
    `abatementrange_low` JSON,
    `abatementrange_high` JSON,
    `abatementstring` STRING,
    `recordeddate` DATETIME,
    `stage` ARRAY<JSON>,
    `evidence` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "onsetstring",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `consent` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `datetime` DATETIME,
    `sourceattachment_extension` ARRAY<JSON>,
    `sourceattachment_contenttype` STRING,
    `sourceattachment_language` STRING,
    `sourceattachment_data` STRING,
    `sourceattachment_url` STRING,
    `sourceattachment_size` INTEGER,
    `sourceattachment_hash` STRING,
    `sourceattachment_title` STRING,
    `sourceattachment_creation` DATETIME,
    `policy` ARRAY<JSON>,
    `verification` ARRAY<JSON>,
    `provision_type` STRING,
    `provision_period` JSON,
    `provision_actor` ARRAY<JSON>,
    `provision_action` JSON,
    `provision_securitylabel` JSON,
    `provision_purpose` JSON,
    `provision_class` JSON,
    `provision_code` JSON,
    `provision_dataperiod` JSON,
    `provision_data` ARRAY<JSON>,
    `provision_provision` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX consent_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `contract` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `status` STRING,
    `instantiatesuri` STRING,
    `issued` DATETIME,
    `applies_extension` ARRAY<JSON>,
    `applies_start` DATETIME,
    `applies_end` DATETIME,
    `name` STRING,
    `title` STRING,
    `subtitle` STRING,
    `alias` ARRAY<STRING>,
    `contentdefinition_type` JSON,
    `contentdefinition_subtype` JSON,
    `contentdefinition_publisher` JSON,
    `contentdefinition_publicationdate` DATETIME,
    `contentdefinition_publicationstatus` STRING,
    `contentdefinition_copyright` STRING,
    `term` ARRAY<JSON>,
    `signer` ARRAY<JSON>,
    `friendly` ARRAY<JSON>,
    `legal` ARRAY<JSON>,
    `rule` ARRAY<JSON>,
    `legallybindingattachment_extension` ARRAY<JSON>,
    `legallybindingattachment_contenttype` STRING,
    `legallybindingattachment_language` STRING,
    `legallybindingattachment_data` STRING,
    `legallybindingattachment_url` STRING,
    `legallybindingattachment_size` INTEGER,
    `legallybindingattachment_hash` STRING,
    `legallybindingattachment_title` STRING,
    `legallybindingattachment_creation` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX contract_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `coverage` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `subscriberid` STRING,
    `dependent` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `class` ARRAY<JSON>,
    `order` INTEGER,
    `network` STRING,
    `costtobeneficiary` ARRAY<JSON>,
    `subrogation` BOOLEAN,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX coverage_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "subscriberid, dependent, network",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `coverageeligibilityrequest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `purpose` ARRAY<STRING>,
    `serviceddate` STRING,
    `servicedperiod_extension` ARRAY<JSON>,
    `servicedperiod_start` DATETIME,
    `servicedperiod_end` DATETIME,
    `created` DATETIME,
    `supportinginfo` ARRAY<JSON>,
    `insurance` ARRAY<JSON>,
    `item` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX coverageeligibilityrequest_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `coverageeligibilityresponse` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `purpose` ARRAY<STRING>,
    `serviceddate` STRING,
    `servicedperiod_extension` ARRAY<JSON>,
    `servicedperiod_start` DATETIME,
    `servicedperiod_end` DATETIME,
    `created` DATETIME,
    `outcome` STRING,
    `disposition` STRING,
    `insurance` ARRAY<JSON>,
    `preauthref` STRING,
    `error` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX coverageeligibilityresponse_status_index (`status`) USING BITMAP,
    INDEX coverageeligibilityresponse_outcome_index (`outcome`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `detectedissue` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `severity` STRING,
    `identifieddatetime` STRING,
    `identifiedperiod_extension` ARRAY<JSON>,
    `identifiedperiod_start` DATETIME,
    `identifiedperiod_end` DATETIME,
    `evidence` ARRAY<JSON>,
    `detail` STRING,
    `reference` STRING,
    `mitigation` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX detectedissue_status_index (`status`) USING BITMAP,
    INDEX detectedissue_severity_index (`severity`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `device` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `udicarrier` ARRAY<JSON>,
    `status` STRING,
    `distinctidentifier` STRING,
    `manufacturer` STRING,
    `manufacturedate` DATETIME,
    `expirationdate` DATETIME,
    `lotnumber` STRING,
    `serialnumber` STRING,
    `devicename` ARRAY<JSON>,
    `modelnumber` STRING,
    `partnumber` STRING,
    `specialization` ARRAY<JSON>,
    `version` ARRAY<JSON>,
    `property` ARRAY<JSON>,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_system` ARRAY<STRING>,
    `contact_value` ARRAY<STRING>,
    `contact_use` ARRAY<STRING>,
    `contact_rank` ARRAY<INTEGER>,
    `contact_period` ARRAY<JSON>,
    `url` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX device_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `devicedefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `udideviceidentifier` ARRAY<JSON>,
    `manufacturerstring` STRING,
    `devicename` ARRAY<JSON>,
    `modelnumber` STRING,
    `specialization` ARRAY<JSON>,
    `version` ARRAY<STRING>,
    `shelflifestorage_extension` ARRAY<ARRAY<JSON>>,
    `shelflifestorage_identifier` ARRAY<JSON>,
    `shelflifestorage_type` ARRAY<JSON>,
    `shelflifestorage_period` ARRAY<JSON>,
    `shelflifestorage_specialprecautionsforstorage` ARRAY<ARRAY<JSON>>,
    `physicalcharacteristics_extension` ARRAY<JSON>,
    `physicalcharacteristics_height` JSON,
    `physicalcharacteristics_width` JSON,
    `physicalcharacteristics_depth` JSON,
    `physicalcharacteristics_weight` JSON,
    `physicalcharacteristics_nominalvolume` JSON,
    `physicalcharacteristics_externaldiameter` JSON,
    `physicalcharacteristics_shape` STRING,
    `physicalcharacteristics_color` ARRAY<STRING>,
    `physicalcharacteristics_imprint` ARRAY<STRING>,
    `physicalcharacteristics_image` ARRAY<JSON>,
    `physicalcharacteristics_scoring` JSON,
    `capability` ARRAY<JSON>,
    `property` ARRAY<JSON>,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_system` ARRAY<STRING>,
    `contact_value` ARRAY<STRING>,
    `contact_use` ARRAY<STRING>,
    `contact_rank` ARRAY<INTEGER>,
    `contact_period` ARRAY<JSON>,
    `url` STRING,
    `onlineinformation` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `quantity_extension` ARRAY<JSON>,
    `quantity_value` DECIMAL(20,10),
    `quantity_comparator` STRING,
    `quantity_unit` STRING,
    `quantity_system` STRING,
    `quantity_code` STRING,
    `material` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `devicemetric` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `operationalstatus` STRING,
    `color` STRING,
    `category` STRING,
    `measurementperiod_extension` ARRAY<JSON>,
    `measurementperiod_event` ARRAY<STRING>,
    `measurementperiod_repeat` JSON,
    `measurementperiod_code` JSON,
    `calibration` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX devicemetric_operationalstatus_index (`operationalstatus`) USING BITMAP,
    INDEX devicemetric_color_index (`color`) USING BITMAP,
    INDEX devicemetric_category_index (`category`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `devicerequest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `status` STRING,
    `intent` STRING,
    `priority` STRING,
    `parameter` ARRAY<JSON>,
    `occurrencedatetime` STRING,
    `occurrenceperiod_extension` ARRAY<JSON>,
    `occurrenceperiod_start` DATETIME,
    `occurrenceperiod_end` DATETIME,
    `occurrencetiming_extension` ARRAY<JSON>,
    `occurrencetiming_event` ARRAY<STRING>,
    `occurrencetiming_repeat` JSON,
    `occurrencetiming_code` JSON,
    `authoredon` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX devicerequest_status_index (`status`) USING BITMAP,
    INDEX devicerequest_intent_index (`intent`) USING BITMAP,
    INDEX devicerequest_priority_index (`priority`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `deviceusestatement` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `timingtiming_extension` ARRAY<JSON>,
    `timingtiming_event` ARRAY<STRING>,
    `timingtiming_repeat` JSON,
    `timingtiming_code` JSON,
    `timingperiod_extension` ARRAY<JSON>,
    `timingperiod_start` DATETIME,
    `timingperiod_end` DATETIME,
    `timingdatetime` STRING,
    `recordedon` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX deviceusestatement_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `diagnosticreport` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `effectivedatetime` STRING,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `issued` STRING,
    `media` ARRAY<JSON>,
    `conclusion` STRING,
    `presentedform_extension` ARRAY<ARRAY<JSON>>,
    `presentedform_contenttype` ARRAY<STRING>,
    `presentedform_language` ARRAY<STRING>,
    `presentedform_data` ARRAY<STRING>,
    `presentedform_url` ARRAY<STRING>,
    `presentedform_size` ARRAY<INTEGER>,
    `presentedform_hash` ARRAY<STRING>,
    `presentedform_title` ARRAY<STRING>,
    `presentedform_creation` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX diagnosticreport_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `documentmanifest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `created` DATETIME,
    `source` STRING,
    `description` STRING,
    `related` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX documentmanifest_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `documentreference` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `docstatus` STRING,
    `date` STRING,
    `relatesto` ARRAY<JSON>,
    `description` STRING,
    `content` ARRAY<JSON>,
    `context_encounter` JSON,
    `context_event` JSON,
    `context_period` JSON,
    `context_facilitytype` JSON,
    `context_practicesetting` JSON,
    `context_sourcepatientinfo` JSON,
    `context_related` JSON,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX documentreference_status_index (`status`) USING BITMAP,
    INDEX documentreference_docstatus_index (`docstatus`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `effectevidencesynthesis` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `samplesize_description` STRING,
    `samplesize_numberofstudies` INTEGER,
    `samplesize_numberofparticipants` INTEGER,
    `resultsbyexposure` ARRAY<JSON>,
    `effectestimate` ARRAY<JSON>,
    `certainty` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX effectevidencesynthesis_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `encounter` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `statushistory` ARRAY<JSON>,
    `classhistory` ARRAY<JSON>,
    `participant` ARRAY<JSON>,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `length_extension` ARRAY<JSON>,
    `length_value` DECIMAL(20,10),
    `length_comparator` STRING,
    `length_unit` STRING,
    `length_system` STRING,
    `length_code` STRING,
    `diagnosis` ARRAY<JSON>,
    `hospitalization_preadmissionidentifier` JSON,
    `hospitalization_origin` JSON,
    `hospitalization_admitsource` JSON,
    `hospitalization_readmission` JSON,
    `hospitalization_dietpreference` JSON,
    `hospitalization_specialcourtesy` JSON,
    `hospitalization_specialarrangement` JSON,
    `hospitalization_destination` JSON,
    `hospitalization_dischargedisposition` JSON,
    `location` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX encounter_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `endpoint` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `name` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_system` ARRAY<STRING>,
    `contact_value` ARRAY<STRING>,
    `contact_use` ARRAY<STRING>,
    `contact_rank` ARRAY<INTEGER>,
    `contact_period` ARRAY<JSON>,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `payloadmimetype` ARRAY<STRING>,
    `address` STRING,
    `header` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX endpoint_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `enrollmentrequest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `created` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX enrollmentrequest_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `enrollmentresponse` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `outcome` STRING,
    `disposition` STRING,
    `created` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX enrollmentresponse_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `episodeofcare` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `statushistory` ARRAY<JSON>,
    `diagnosis` ARRAY<JSON>,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX episodeofcare_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `eventdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `subtitle` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `usage` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `trigger_extension` ARRAY<ARRAY<JSON>>,
    `trigger_type` ARRAY<STRING>,
    `trigger_name` ARRAY<STRING>,
    `trigger_timingtiming` ARRAY<JSON>,
    `trigger_timingreference` ARRAY<JSON>,
    `trigger_timingdate` ARRAY<STRING>,
    `trigger_timingdatetime` ARRAY<STRING>,
    `trigger_data` ARRAY<ARRAY<JSON>>,
    `trigger_condition` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX eventdefinition_status_index (`status`) USING BITMAP,
    INDEX eventdefinition_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `evidence` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `shorttitle` STRING,
    `subtitle` STRING,
    `status` STRING,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX evidence_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `evidencevariable` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `shorttitle` STRING,
    `subtitle` STRING,
    `status` STRING,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `type` STRING,
    `characteristic` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX evidencevariable_status_index (`status`) USING BITMAP,
    INDEX evidencevariable_type_index (`type`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, shorttitle, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `examplescenario` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `copyright` STRING,
    `purpose` STRING,
    `actor` ARRAY<JSON>,
    `instance` ARRAY<JSON>,
    `process` ARRAY<JSON>,
    `workflow` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX examplescenario_status_index (`status`) USING BITMAP,
    INDEX examplescenario_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `explanationofbenefit` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `use` STRING,
    `billableperiod_extension` ARRAY<JSON>,
    `billableperiod_start` DATETIME,
    `billableperiod_end` DATETIME,
    `created` DATETIME,
    `related` ARRAY<JSON>,
    `payee_type` JSON,
    `payee_party` JSON,
    `outcome` STRING,
    `disposition` STRING,
    `preauthref` ARRAY<STRING>,
    `preauthrefperiod_extension` ARRAY<ARRAY<JSON>>,
    `preauthrefperiod_start` ARRAY<STRING>,
    `preauthrefperiod_end` ARRAY<STRING>,
    `careteam` ARRAY<JSON>,
    `supportinginfo` ARRAY<JSON>,
    `diagnosis` ARRAY<JSON>,
    `procedure` ARRAY<JSON>,
    `precedence` INTEGER,
    `insurance` ARRAY<JSON>,
    `accident_date` DATETIME,
    `accident_type` JSON,
    `accident_locationaddress` JSON,
    `accident_locationreference` JSON,
    `item` ARRAY<JSON>,
    `additem` ARRAY<JSON>,
    `adjudication` ARRAY<JSON>,
    `total` ARRAY<JSON>,
    `payment_type` JSON,
    `payment_adjustment` JSON,
    `payment_adjustmentreason` JSON,
    `payment_date` DATETIME,
    `payment_amount` JSON,
    `payment_identifier` JSON,
    `form_extension` ARRAY<JSON>,
    `form_contenttype` STRING,
    `form_language` STRING,
    `form_data` STRING,
    `form_url` STRING,
    `form_size` INTEGER,
    `form_hash` STRING,
    `form_title` STRING,
    `form_creation` DATETIME,
    `processnote` ARRAY<JSON>,
    `benefitperiod_extension` ARRAY<JSON>,
    `benefitperiod_start` DATETIME,
    `benefitperiod_end` DATETIME,
    `benefitbalance` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX explanationofbenefit_status_index (`status`) USING BITMAP,
    INDEX explanationofbenefit_use_index (`use`) USING BITMAP,
    INDEX explanationofbenefit_outcome_index (`outcome`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `familymemberhistory` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `status` STRING,
    `date` DATETIME,
    `name` STRING,
    `bornperiod_extension` ARRAY<JSON>,
    `bornperiod_start` DATETIME,
    `bornperiod_end` DATETIME,
    `borndate` STRING,
    `bornstring` STRING,
    `ageage_extension` ARRAY<JSON>,
    `ageage_value` DECIMAL(20,10),
    `ageage_comparator` STRING,
    `ageage_unit` STRING,
    `ageage_system` STRING,
    `ageage_code` STRING,
    `agerange_extension` ARRAY<JSON>,
    `agerange_low` JSON,
    `agerange_high` JSON,
    `agestring` STRING,
    `estimatedage` BOOLEAN,
    `deceasedboolean` BOOLEAN,
    `deceasedage_extension` ARRAY<JSON>,
    `deceasedage_value` DECIMAL(20,10),
    `deceasedage_comparator` STRING,
    `deceasedage_unit` STRING,
    `deceasedage_system` STRING,
    `deceasedage_code` STRING,
    `deceasedrange_extension` ARRAY<JSON>,
    `deceasedrange_low` JSON,
    `deceasedrange_high` JSON,
    `deceaseddate` STRING,
    `deceasedstring` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `condition` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX familymemberhistory_status_index (`status`) USING BITMAP,
    INDEX familymemberhistory_estimatedage_index (`estimatedage`) USING BITMAP,
    INDEX familymemberhistory_deceasedboolean_index (`deceasedboolean`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name, agestring, deceasedstring",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `flag` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX flag_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `goal` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `lifecyclestatus` STRING,
    `startdate` STRING,
    `target` ARRAY<JSON>,
    `statusdate` DATETIME,
    `statusreason` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX goal_lifecyclestatus_index (`lifecyclestatus`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `graphdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `start` STRING,
    `profile` STRING,
    `link` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX graphdefinition_status_index (`status`) USING BITMAP,
    INDEX graphdefinition_experimental_index (`experimental`) USING BITMAP,
    INDEX graphdefinition_start_index (`start`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `group` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `type` STRING,
    `actual` BOOLEAN,
    `name` STRING,
    `quantity` INTEGER,
    `characteristic` ARRAY<JSON>,
    `member` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX group_active_index (`active`) USING BITMAP,
    INDEX group_type_index (`type`) USING BITMAP,
    INDEX group_actual_index (`actual`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `guidanceresponse` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `moduleuri` STRING,
    `modulecanonical` STRING,
    `status` STRING,
    `occurrencedatetime` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `datarequirement_extension` ARRAY<ARRAY<JSON>>,
    `datarequirement_type` ARRAY<STRING>,
    `datarequirement_profile` ARRAY<ARRAY<STRING>>,
    `datarequirement_subjectcodeableconcept` ARRAY<JSON>,
    `datarequirement_subjectreference` ARRAY<JSON>,
    `datarequirement_mustsupport` ARRAY<ARRAY<STRING>>,
    `datarequirement_codefilter` ARRAY<ARRAY<JSON>>,
    `datarequirement_datefilter` ARRAY<ARRAY<JSON>>,
    `datarequirement_limit` ARRAY<INTEGER>,
    `datarequirement_sort` ARRAY<ARRAY<JSON>>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX guidanceresponse_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `healthcareservice` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `name` STRING,
    `comment` STRING,
    `extradetails` STRING,
    `photo_extension` ARRAY<JSON>,
    `photo_contenttype` STRING,
    `photo_language` STRING,
    `photo_data` STRING,
    `photo_url` STRING,
    `photo_size` INTEGER,
    `photo_hash` STRING,
    `photo_title` STRING,
    `photo_creation` DATETIME,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `eligibility` ARRAY<JSON>,
    `appointmentrequired` BOOLEAN,
    `availabletime` ARRAY<JSON>,
    `notavailable` ARRAY<JSON>,
    `availabilityexceptions` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX healthcareservice_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name, comment",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `imagingstudy` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `started` DATETIME,
    `numberofseries` INTEGER,
    `numberofinstances` INTEGER,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `description` STRING,
    `series` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX imagingstudy_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `immunization` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `occurrencedatetime` STRING,
    `occurrencestring` STRING,
    `recorded` DATETIME,
    `primarysource` BOOLEAN,
    `lotnumber` STRING,
    `expirationdate` DATETIME,
    `dosequantity_extension` ARRAY<JSON>,
    `dosequantity_value` DECIMAL(20,10),
    `dosequantity_comparator` STRING,
    `dosequantity_unit` STRING,
    `dosequantity_system` STRING,
    `dosequantity_code` STRING,
    `performer` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `issubpotent` BOOLEAN,
    `education` ARRAY<JSON>,
    `reaction` ARRAY<JSON>,
    `protocolapplied` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX immunization_status_index (`status`) USING BITMAP,
    INDEX immunization_primarysource_index (`primarysource`) USING BITMAP,
    INDEX immunization_issubpotent_index (`issubpotent`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "occurrencestring",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `immunizationevaluation` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `date` DATETIME,
    `description` STRING,
    `series` STRING,
    `dosenumberpositiveint` INTEGER,
    `dosenumberstring` STRING,
    `seriesdosespositiveint` INTEGER,
    `seriesdosesstring` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX immunizationevaluation_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `immunizationrecommendation` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `date` DATETIME,
    `recommendation` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `implementationguide` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `copyright` STRING,
    `packageid` VARCHAR(100),
    `license` STRING,
    `fhirversion` ARRAY<STRING>,
    `dependson` ARRAY<JSON>,
    `global` ARRAY<JSON>,
    `definition_grouping` ARRAY<JSON>,
    `definition_resource` ARRAY<JSON>,
    `definition_page` JSON,
    `definition_parameter` ARRAY<JSON>,
    `definition_template` ARRAY<JSON>,
    `manifest_rendering` STRING,
    `manifest_resource` ARRAY<JSON>,
    `manifest_page` ARRAY<JSON>,
    `manifest_image` ARRAY<STRING>,
    `manifest_other` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX implementationguide_status_index (`status`) USING BITMAP,
    INDEX implementationguide_experimental_index (`experimental`) USING BITMAP,
    INDEX implementationguide_license_index (`license`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `insuranceplan` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `name` STRING,
    `alias` ARRAY<STRING>,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `contact` ARRAY<JSON>,
    `coverage` ARRAY<JSON>,
    `plan` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX insuranceplan_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `invoice` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `cancelledreason` STRING,
    `date` DATETIME,
    `participant` ARRAY<JSON>,
    `lineitem` ARRAY<JSON>,
    `totalpricecomponent` ARRAY<JSON>,
    `totalnet_extension` ARRAY<JSON>,
    `totalnet_value` DECIMAL(20,10),
    `totalnet_currency` STRING,
    `totalgross_extension` ARRAY<JSON>,
    `totalgross_value` DECIMAL(20,10),
    `totalgross_currency` STRING,
    `paymentterms` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX invoice_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `library` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `subtitle` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `usage` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `parameter_extension` ARRAY<ARRAY<JSON>>,
    `parameter_name` ARRAY<STRING>,
    `parameter_use` ARRAY<STRING>,
    `parameter_min` ARRAY<INTEGER>,
    `parameter_max` ARRAY<STRING>,
    `parameter_documentation` ARRAY<STRING>,
    `parameter_type` ARRAY<STRING>,
    `parameter_profile` ARRAY<STRING>,
    `datarequirement_extension` ARRAY<ARRAY<JSON>>,
    `datarequirement_type` ARRAY<STRING>,
    `datarequirement_profile` ARRAY<ARRAY<STRING>>,
    `datarequirement_subjectcodeableconcept` ARRAY<JSON>,
    `datarequirement_subjectreference` ARRAY<JSON>,
    `datarequirement_mustsupport` ARRAY<ARRAY<STRING>>,
    `datarequirement_codefilter` ARRAY<ARRAY<JSON>>,
    `datarequirement_datefilter` ARRAY<ARRAY<JSON>>,
    `datarequirement_limit` ARRAY<INTEGER>,
    `datarequirement_sort` ARRAY<ARRAY<JSON>>,
    `content_extension` ARRAY<ARRAY<JSON>>,
    `content_contenttype` ARRAY<STRING>,
    `content_language` ARRAY<STRING>,
    `content_data` ARRAY<STRING>,
    `content_url` ARRAY<STRING>,
    `content_size` ARRAY<INTEGER>,
    `content_hash` ARRAY<STRING>,
    `content_title` ARRAY<STRING>,
    `content_creation` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX library_status_index (`status`) USING BITMAP,
    INDEX library_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `linkage` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `item` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX linkage_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `list` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `mode` STRING,
    `title` STRING,
    `date` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `entry` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX list_status_index (`status`) USING BITMAP,
    INDEX list_mode_index (`mode`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "title",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `location` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `name` STRING,
    `alias` ARRAY<STRING>,
    `description` STRING,
    `mode` STRING,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `address_extension` ARRAY<JSON>,
    `address_use` STRING,
    `address_type` STRING,
    `address_text` STRING,
    `address_line` ARRAY<STRING>,
    `address_city` STRING,
    `address_district` STRING,
    `address_state` STRING,
    `address_postalcode` STRING,
    `address_country` STRING,
    `address_period` JSON,
    `position_longitude` DECIMAL(20,10),
    `position_latitude` DECIMAL(20,10),
    `position_altitude` DECIMAL(20,10),
    `hoursofoperation` ARRAY<JSON>,
    `availabilityexceptions` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX location_status_index (`status`) USING BITMAP,
    INDEX location_mode_index (`mode`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name, description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `measure` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `subtitle` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `usage` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `library` ARRAY<STRING>,
    `disclaimer` STRING,
    `riskadjustment` STRING,
    `rateaggregation` STRING,
    `rationale` STRING,
    `clinicalrecommendationstatement` STRING,
    `definition` ARRAY<STRING>,
    `guidance` STRING,
    `group` ARRAY<JSON>,
    `supplementaldata` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX measure_status_index (`status`) USING BITMAP,
    INDEX measure_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher, riskadjustment, rateaggregation",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `measurereport` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `type` STRING,
    `measure` STRING,
    `date` DATETIME,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `group` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX measurereport_status_index (`status`) USING BITMAP,
    INDEX measurereport_type_index (`type`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `media` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `createddatetime` STRING,
    `createdperiod_extension` ARRAY<JSON>,
    `createdperiod_start` DATETIME,
    `createdperiod_end` DATETIME,
    `issued` STRING,
    `devicename` STRING,
    `height` INTEGER,
    `width` INTEGER,
    `frames` INTEGER,
    `duration` DECIMAL(20,10),
    `content_extension` ARRAY<JSON>,
    `content_contenttype` STRING,
    `content_language` STRING,
    `content_data` STRING,
    `content_url` STRING,
    `content_size` INTEGER,
    `content_hash` STRING,
    `content_title` STRING,
    `content_creation` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX media_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "devicename",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medication` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `amount_extension` ARRAY<JSON>,
    `amount_numerator` JSON,
    `amount_denominator` JSON,
    `ingredient` ARRAY<JSON>,
    `batch_lotnumber` STRING,
    `batch_expirationdate` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX medication_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicationadministration` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiates` ARRAY<STRING>,
    `status` STRING,
    `effectivedatetime` STRING,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `performer` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `dosage_text` STRING,
    `dosage_site` JSON,
    `dosage_route` JSON,
    `dosage_method` JSON,
    `dosage_dose` JSON,
    `dosage_rateratio` JSON,
    `dosage_ratequantity` JSON,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX medicationadministration_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicationdispense` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `performer` ARRAY<JSON>,
    `quantity_extension` ARRAY<JSON>,
    `quantity_value` DECIMAL(20,10),
    `quantity_comparator` STRING,
    `quantity_unit` STRING,
    `quantity_system` STRING,
    `quantity_code` STRING,
    `dayssupply_extension` ARRAY<JSON>,
    `dayssupply_value` DECIMAL(20,10),
    `dayssupply_comparator` STRING,
    `dayssupply_unit` STRING,
    `dayssupply_system` STRING,
    `dayssupply_code` STRING,
    `whenprepared` DATETIME,
    `whenhandedover` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `dosageinstruction_extension` ARRAY<ARRAY<JSON>>,
    `dosageinstruction_sequence` ARRAY<INTEGER>,
    `dosageinstruction_text` ARRAY<STRING>,
    `dosageinstruction_additionalinstruction` ARRAY<ARRAY<JSON>>,
    `dosageinstruction_patientinstruction` ARRAY<STRING>,
    `dosageinstruction_timing` ARRAY<JSON>,
    `dosageinstruction_asneededboolean` ARRAY<BOOLEAN>,
    `dosageinstruction_asneededcodeableconcept` ARRAY<JSON>,
    `dosageinstruction_site` ARRAY<JSON>,
    `dosageinstruction_route` ARRAY<JSON>,
    `dosageinstruction_method` ARRAY<JSON>,
    `dosageinstruction_doseandrate` ARRAY<ARRAY<JSON>>,
    `dosageinstruction_maxdoseperperiod` ARRAY<JSON>,
    `dosageinstruction_maxdoseperadministration` ARRAY<JSON>,
    `dosageinstruction_maxdoseperlifetime` ARRAY<JSON>,
    `substitution_wassubstituted` BOOLEAN,
    `substitution_type` JSON,
    `substitution_reason` JSON,
    `substitution_responsibleparty` JSON,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX medicationdispense_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicationknowledge` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `amount_extension` ARRAY<JSON>,
    `amount_value` DECIMAL(20,10),
    `amount_comparator` STRING,
    `amount_unit` STRING,
    `amount_system` STRING,
    `amount_code` STRING,
    `synonym` ARRAY<STRING>,
    `relatedmedicationknowledge` ARRAY<JSON>,
    `monograph` ARRAY<JSON>,
    `ingredient` ARRAY<JSON>,
    `preparationinstruction` STRING,
    `cost` ARRAY<JSON>,
    `monitoringprogram` ARRAY<JSON>,
    `administrationguidelines` ARRAY<JSON>,
    `medicineclassification` ARRAY<JSON>,
    `packaging_type` JSON,
    `packaging_quantity` JSON,
    `drugcharacteristic` ARRAY<JSON>,
    `regulatory` ARRAY<JSON>,
    `kinetics` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX medicationknowledge_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicationrequest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `intent` STRING,
    `priority` STRING,
    `donotperform` BOOLEAN,
    `reportedboolean` BOOLEAN,
    `authoredon` DATETIME,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `dosageinstruction_extension` ARRAY<ARRAY<JSON>>,
    `dosageinstruction_sequence` ARRAY<INTEGER>,
    `dosageinstruction_text` ARRAY<STRING>,
    `dosageinstruction_additionalinstruction` ARRAY<ARRAY<JSON>>,
    `dosageinstruction_patientinstruction` ARRAY<STRING>,
    `dosageinstruction_timing` ARRAY<JSON>,
    `dosageinstruction_asneededboolean` ARRAY<BOOLEAN>,
    `dosageinstruction_asneededcodeableconcept` ARRAY<JSON>,
    `dosageinstruction_site` ARRAY<JSON>,
    `dosageinstruction_route` ARRAY<JSON>,
    `dosageinstruction_method` ARRAY<JSON>,
    `dosageinstruction_doseandrate` ARRAY<ARRAY<JSON>>,
    `dosageinstruction_maxdoseperperiod` ARRAY<JSON>,
    `dosageinstruction_maxdoseperadministration` ARRAY<JSON>,
    `dosageinstruction_maxdoseperlifetime` ARRAY<JSON>,
    `dispenserequest_initialfill` JSON,
    `dispenserequest_dispenseinterval` JSON,
    `dispenserequest_validityperiod` JSON,
    `dispenserequest_numberofrepeatsallowed` INTEGER,
    `dispenserequest_quantity` JSON,
    `dispenserequest_expectedsupplyduration` JSON,
    `dispenserequest_performer` JSON,
    `substitution_allowedboolean` BOOLEAN,
    `substitution_allowedcodeableconcept` JSON,
    `substitution_reason` JSON,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX medicationrequest_status_index (`status`) USING BITMAP,
    INDEX medicationrequest_intent_index (`intent`) USING BITMAP,
    INDEX medicationrequest_priority_index (`priority`) USING BITMAP,
    INDEX medicationrequest_donotperform_index (`donotperform`) USING BITMAP,
    INDEX medicationrequest_reportedboolean_index (`reportedboolean`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicationstatement` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `effectivedatetime` STRING,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `dateasserted` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `dosage_extension` ARRAY<ARRAY<JSON>>,
    `dosage_sequence` ARRAY<INTEGER>,
    `dosage_text` ARRAY<STRING>,
    `dosage_additionalinstruction` ARRAY<ARRAY<JSON>>,
    `dosage_patientinstruction` ARRAY<STRING>,
    `dosage_timing` ARRAY<JSON>,
    `dosage_asneededboolean` ARRAY<BOOLEAN>,
    `dosage_asneededcodeableconcept` ARRAY<JSON>,
    `dosage_site` ARRAY<JSON>,
    `dosage_route` ARRAY<JSON>,
    `dosage_method` ARRAY<JSON>,
    `dosage_doseandrate` ARRAY<ARRAY<JSON>>,
    `dosage_maxdoseperperiod` ARRAY<JSON>,
    `dosage_maxdoseperadministration` ARRAY<JSON>,
    `dosage_maxdoseperlifetime` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX medicationstatement_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproduct` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `specialmeasures` ARRAY<STRING>,
    `marketingstatus_extension` ARRAY<ARRAY<JSON>>,
    `marketingstatus_country` ARRAY<JSON>,
    `marketingstatus_jurisdiction` ARRAY<JSON>,
    `marketingstatus_status` ARRAY<JSON>,
    `marketingstatus_daterange` ARRAY<JSON>,
    `marketingstatus_restoredate` ARRAY<STRING>,
    `name` ARRAY<JSON>,
    `manufacturingbusinessoperation` ARRAY<JSON>,
    `specialdesignation` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductauthorization` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `statusdate` DATETIME,
    `restoredate` DATETIME,
    `validityperiod_extension` ARRAY<JSON>,
    `validityperiod_start` DATETIME,
    `validityperiod_end` DATETIME,
    `dataexclusivityperiod_extension` ARRAY<JSON>,
    `dataexclusivityperiod_start` DATETIME,
    `dataexclusivityperiod_end` DATETIME,
    `dateoffirstauthorization` DATETIME,
    `internationalbirthdate` DATETIME,
    `jurisdictionalauthorization` ARRAY<JSON>,
    `procedure_identifier` JSON,
    `procedure_type` JSON,
    `procedure_dateperiod` JSON,
    `procedure_datedatetime` STRING,
    `procedure_application` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductcontraindication` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `othertherapy` ARRAY<JSON>,
    `population_extension` ARRAY<ARRAY<JSON>>,
    `population_agerange` ARRAY<JSON>,
    `population_agecodeableconcept` ARRAY<JSON>,
    `population_gender` ARRAY<JSON>,
    `population_race` ARRAY<JSON>,
    `population_physiologicalcondition` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductindication` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `duration_extension` ARRAY<JSON>,
    `duration_value` DECIMAL(20,10),
    `duration_comparator` STRING,
    `duration_unit` STRING,
    `duration_system` STRING,
    `duration_code` STRING,
    `othertherapy` ARRAY<JSON>,
    `population_extension` ARRAY<ARRAY<JSON>>,
    `population_agerange` ARRAY<JSON>,
    `population_agecodeableconcept` ARRAY<JSON>,
    `population_gender` ARRAY<JSON>,
    `population_race` ARRAY<JSON>,
    `population_physiologicalcondition` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductingredient` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `allergenicindicator` BOOLEAN,
    `specifiedsubstance` ARRAY<JSON>,
    `substance_code` JSON,
    `substance_strength` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX medicinalproductingredient_allergenicindicator_index (`allergenicindicator`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductinteraction` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `description` STRING,
    `interactant` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductmanufactured` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `quantity_extension` ARRAY<JSON>,
    `quantity_value` DECIMAL(20,10),
    `quantity_comparator` STRING,
    `quantity_unit` STRING,
    `quantity_system` STRING,
    `quantity_code` STRING,
    `physicalcharacteristics_extension` ARRAY<JSON>,
    `physicalcharacteristics_height` JSON,
    `physicalcharacteristics_width` JSON,
    `physicalcharacteristics_depth` JSON,
    `physicalcharacteristics_weight` JSON,
    `physicalcharacteristics_nominalvolume` JSON,
    `physicalcharacteristics_externaldiameter` JSON,
    `physicalcharacteristics_shape` STRING,
    `physicalcharacteristics_color` ARRAY<STRING>,
    `physicalcharacteristics_imprint` ARRAY<STRING>,
    `physicalcharacteristics_image` ARRAY<JSON>,
    `physicalcharacteristics_scoring` JSON,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductpackaged` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `description` STRING,
    `marketingstatus_extension` ARRAY<ARRAY<JSON>>,
    `marketingstatus_country` ARRAY<JSON>,
    `marketingstatus_jurisdiction` ARRAY<JSON>,
    `marketingstatus_status` ARRAY<JSON>,
    `marketingstatus_daterange` ARRAY<JSON>,
    `marketingstatus_restoredate` ARRAY<STRING>,
    `batchidentifier` ARRAY<JSON>,
    `packageitem` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductpharmaceutical` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `characteristics` ARRAY<JSON>,
    `routeofadministration` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `medicinalproductundesirableeffect` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `population_extension` ARRAY<ARRAY<JSON>>,
    `population_agerange` ARRAY<JSON>,
    `population_agecodeableconcept` ARRAY<JSON>,
    `population_gender` ARRAY<JSON>,
    `population_race` ARRAY<JSON>,
    `population_physiologicalcondition` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `messagedefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `replaces` ARRAY<STRING>,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `base` STRING,
    `parent` ARRAY<STRING>,
    `eventuri` STRING,
    `category` STRING,
    `focus` ARRAY<JSON>,
    `responserequired` STRING,
    `allowedresponse` ARRAY<JSON>,
    `graph` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX messagedefinition_status_index (`status`) USING BITMAP,
    INDEX messagedefinition_experimental_index (`experimental`) USING BITMAP,
    INDEX messagedefinition_category_index (`category`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `messageheader` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `eventuri` STRING,
    `destination` ARRAY<JSON>,
    `source_name` STRING,
    `source_software` STRING,
    `source_version` STRING,
    `source_contact` JSON,
    `source_endpoint` STRING,
    `response_identifier` VARCHAR(100),
    `response_code` STRING,
    `response_details` JSON,
    `definition` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `molecularsequence` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `type` STRING,
    `coordinatesystem` INTEGER,
    `quantity_extension` ARRAY<JSON>,
    `quantity_value` DECIMAL(20,10),
    `quantity_comparator` STRING,
    `quantity_unit` STRING,
    `quantity_system` STRING,
    `quantity_code` STRING,
    `referenceseq_chromosome` JSON,
    `referenceseq_genomebuild` STRING,
    `referenceseq_orientation` STRING,
    `referenceseq_referenceseqid` JSON,
    `referenceseq_referenceseqpointer` JSON,
    `referenceseq_referenceseqstring` STRING,
    `referenceseq_strand` STRING,
    `referenceseq_windowstart` INTEGER,
    `referenceseq_windowend` INTEGER,
    `variant` ARRAY<JSON>,
    `observedseq` STRING,
    `quality` ARRAY<JSON>,
    `readcoverage` INTEGER,
    `repository` ARRAY<JSON>,
    `structurevariant` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX molecularsequence_type_index (`type`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "observedseq",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `namingsystem` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `name` STRING,
    `status` STRING,
    `kind` STRING,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `responsible` STRING,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `usage` STRING,
    `uniqueid` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX namingsystem_status_index (`status`) USING BITMAP,
    INDEX namingsystem_kind_index (`kind`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `nutritionorder` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `instantiates` ARRAY<STRING>,
    `status` STRING,
    `intent` STRING,
    `datetime` DATETIME,
    `oraldiet_type` JSON,
    `oraldiet_schedule` JSON,
    `oraldiet_nutrient` ARRAY<JSON>,
    `oraldiet_texture` ARRAY<JSON>,
    `oraldiet_fluidconsistencytype` JSON,
    `oraldiet_instruction` STRING,
    `supplement` ARRAY<JSON>,
    `enteralformula_baseformulatype` JSON,
    `enteralformula_baseformulaproductname` STRING,
    `enteralformula_additivetype` JSON,
    `enteralformula_additiveproductname` STRING,
    `enteralformula_caloricdensity` JSON,
    `enteralformula_routeofadministration` JSON,
    `enteralformula_administration` ARRAY<JSON>,
    `enteralformula_maxvolumetodeliver` JSON,
    `enteralformula_administrationinstruction` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX nutritionorder_status_index (`status`) USING BITMAP,
    INDEX nutritionorder_intent_index (`intent`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `observation` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `effectivedatetime` STRING,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `effectivetiming_extension` ARRAY<JSON>,
    `effectivetiming_event` ARRAY<STRING>,
    `effectivetiming_repeat` JSON,
    `effectivetiming_code` JSON,
    `effectiveinstant` STRING,
    `issued` STRING,
    `valuequantity_extension` ARRAY<JSON>,
    `valuequantity_value` DECIMAL(20,10),
    `valuequantity_comparator` STRING,
    `valuequantity_unit` STRING,
    `valuequantity_system` STRING,
    `valuequantity_code` STRING,
    `valuestring` STRING,
    `valueboolean` BOOLEAN,
    `valueinteger` INTEGER,
    `valuerange_extension` ARRAY<JSON>,
    `valuerange_low` JSON,
    `valuerange_high` JSON,
    `valueratio_extension` ARRAY<JSON>,
    `valueratio_numerator` JSON,
    `valueratio_denominator` JSON,
    `valuesampleddata_extension` ARRAY<JSON>,
    `valuesampleddata_origin` JSON,
    `valuesampleddata_period` DECIMAL(20,10),
    `valuesampleddata_factor` DECIMAL(20,10),
    `valuesampleddata_lowerlimit` DECIMAL(20,10),
    `valuesampleddata_upperlimit` DECIMAL(20,10),
    `valuesampleddata_dimensions` INTEGER,
    `valuesampleddata_data` STRING,
    `valuetime` STRING,
    `valuedatetime` STRING,
    `valueperiod_extension` ARRAY<JSON>,
    `valueperiod_start` DATETIME,
    `valueperiod_end` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `referencerange` ARRAY<JSON>,
    `component` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX observation_status_index (`status`) USING BITMAP,
    INDEX observation_valueboolean_index (`valueboolean`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "valuestring",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `observationdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `permitteddatatype` ARRAY<STRING>,
    `multipleresultsallowed` BOOLEAN,
    `preferredreportname` STRING,
    `quantitativedetails_customaryunit` JSON,
    `quantitativedetails_unit` JSON,
    `quantitativedetails_conversionfactor` DECIMAL(20,10),
    `quantitativedetails_decimalprecision` INTEGER,
    `qualifiedinterval` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `operationdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `kind` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `affectsstate` BOOLEAN,
    `code` STRING,
    `comment` STRING,
    `base` STRING,
    `resource` ARRAY<STRING>,
    `system` BOOLEAN,
    `type` BOOLEAN,
    `instance` BOOLEAN,
    `inputprofile` STRING,
    `outputprofile` STRING,
    `parameter` ARRAY<JSON>,
    `overload` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX operationdefinition_status_index (`status`) USING BITMAP,
    INDEX operationdefinition_kind_index (`kind`) USING BITMAP,
    INDEX operationdefinition_experimental_index (`experimental`) USING BITMAP,
    INDEX operationdefinition_affectsstate_index (`affectsstate`) USING BITMAP,
    INDEX operationdefinition_code_index (`code`) USING BITMAP,
    INDEX operationdefinition_system_index (`system`) USING BITMAP,
    INDEX operationdefinition_type_index (`type`) USING BITMAP,
    INDEX operationdefinition_instance_index (`instance`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `operationoutcome` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `issue` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `organization` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `name` STRING,
    `alias` ARRAY<STRING>,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `address_extension` ARRAY<ARRAY<JSON>>,
    `address_use` ARRAY<STRING>,
    `address_type` ARRAY<STRING>,
    `address_text` ARRAY<STRING>,
    `address_line` ARRAY<ARRAY<STRING>>,
    `address_city` ARRAY<STRING>,
    `address_district` ARRAY<STRING>,
    `address_state` ARRAY<STRING>,
    `address_postalcode` ARRAY<STRING>,
    `address_country` ARRAY<STRING>,
    `address_period` ARRAY<JSON>,
    `contact` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX organization_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `organizationaffiliation` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX organizationaffiliation_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `parameters` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `parameter` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `patient` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `name_extension` ARRAY<ARRAY<JSON>>,
    `name_use` ARRAY<STRING>,
    `name_text` ARRAY<STRING>,
    `name_family` ARRAY<STRING>,
    `name_given` ARRAY<ARRAY<STRING>>,
    `name_prefix` ARRAY<ARRAY<STRING>>,
    `name_suffix` ARRAY<ARRAY<STRING>>,
    `name_period` ARRAY<JSON>,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `gender` STRING,
    `birthdate` DATETIME,
    `deceasedboolean` BOOLEAN,
    `deceaseddatetime` STRING,
    `address_extension` ARRAY<ARRAY<JSON>>,
    `address_use` ARRAY<STRING>,
    `address_type` ARRAY<STRING>,
    `address_text` ARRAY<STRING>,
    `address_line` ARRAY<ARRAY<STRING>>,
    `address_city` ARRAY<STRING>,
    `address_district` ARRAY<STRING>,
    `address_state` ARRAY<STRING>,
    `address_postalcode` ARRAY<STRING>,
    `address_country` ARRAY<STRING>,
    `address_period` ARRAY<JSON>,
    `multiplebirthboolean` BOOLEAN,
    `multiplebirthinteger` INTEGER,
    `photo_extension` ARRAY<ARRAY<JSON>>,
    `photo_contenttype` ARRAY<STRING>,
    `photo_language` ARRAY<STRING>,
    `photo_data` ARRAY<STRING>,
    `photo_url` ARRAY<STRING>,
    `photo_size` ARRAY<INTEGER>,
    `photo_hash` ARRAY<STRING>,
    `photo_title` ARRAY<STRING>,
    `photo_creation` ARRAY<STRING>,
    `contact` ARRAY<JSON>,
    `communication` ARRAY<JSON>,
    `link` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX patient_active_index (`active`) USING BITMAP,
    INDEX patient_gender_index (`gender`) USING BITMAP,
    INDEX patient_deceasedboolean_index (`deceasedboolean`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `paymentnotice` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `created` DATETIME,
    `paymentdate` DATETIME,
    `amount_extension` ARRAY<JSON>,
    `amount_value` DECIMAL(20,10),
    `amount_currency` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX paymentnotice_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `paymentreconciliation` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `created` DATETIME,
    `outcome` STRING,
    `disposition` STRING,
    `paymentdate` DATETIME,
    `paymentamount_extension` ARRAY<JSON>,
    `paymentamount_value` DECIMAL(20,10),
    `paymentamount_currency` STRING,
    `detail` ARRAY<JSON>,
    `processnote` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX paymentreconciliation_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `person` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `name_extension` ARRAY<ARRAY<JSON>>,
    `name_use` ARRAY<STRING>,
    `name_text` ARRAY<STRING>,
    `name_family` ARRAY<STRING>,
    `name_given` ARRAY<ARRAY<STRING>>,
    `name_prefix` ARRAY<ARRAY<STRING>>,
    `name_suffix` ARRAY<ARRAY<STRING>>,
    `name_period` ARRAY<JSON>,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `gender` STRING,
    `birthdate` DATETIME,
    `address_extension` ARRAY<ARRAY<JSON>>,
    `address_use` ARRAY<STRING>,
    `address_type` ARRAY<STRING>,
    `address_text` ARRAY<STRING>,
    `address_line` ARRAY<ARRAY<STRING>>,
    `address_city` ARRAY<STRING>,
    `address_district` ARRAY<STRING>,
    `address_state` ARRAY<STRING>,
    `address_postalcode` ARRAY<STRING>,
    `address_country` ARRAY<STRING>,
    `address_period` ARRAY<JSON>,
    `photo_extension` ARRAY<JSON>,
    `photo_contenttype` STRING,
    `photo_language` STRING,
    `photo_data` STRING,
    `photo_url` STRING,
    `photo_size` INTEGER,
    `photo_hash` STRING,
    `photo_title` STRING,
    `photo_creation` DATETIME,
    `active` BOOLEAN,
    `link` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX person_gender_index (`gender`) USING BITMAP,
    INDEX person_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `plandefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `subtitle` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `usage` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `library` ARRAY<STRING>,
    `goal` ARRAY<JSON>,
    `action` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX plandefinition_status_index (`status`) USING BITMAP,
    INDEX plandefinition_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `practitioner` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `name_extension` ARRAY<ARRAY<JSON>>,
    `name_use` ARRAY<STRING>,
    `name_text` ARRAY<STRING>,
    `name_family` ARRAY<STRING>,
    `name_given` ARRAY<ARRAY<STRING>>,
    `name_prefix` ARRAY<ARRAY<STRING>>,
    `name_suffix` ARRAY<ARRAY<STRING>>,
    `name_period` ARRAY<JSON>,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `address_extension` ARRAY<ARRAY<JSON>>,
    `address_use` ARRAY<STRING>,
    `address_type` ARRAY<STRING>,
    `address_text` ARRAY<STRING>,
    `address_line` ARRAY<ARRAY<STRING>>,
    `address_city` ARRAY<STRING>,
    `address_district` ARRAY<STRING>,
    `address_state` ARRAY<STRING>,
    `address_postalcode` ARRAY<STRING>,
    `address_country` ARRAY<STRING>,
    `address_period` ARRAY<JSON>,
    `gender` STRING,
    `birthdate` DATETIME,
    `photo_extension` ARRAY<ARRAY<JSON>>,
    `photo_contenttype` ARRAY<STRING>,
    `photo_language` ARRAY<STRING>,
    `photo_data` ARRAY<STRING>,
    `photo_url` ARRAY<STRING>,
    `photo_size` ARRAY<INTEGER>,
    `photo_hash` ARRAY<STRING>,
    `photo_title` ARRAY<STRING>,
    `photo_creation` ARRAY<STRING>,
    `qualification` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX practitioner_active_index (`active`) USING BITMAP,
    INDEX practitioner_gender_index (`gender`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `practitionerrole` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `availabletime` ARRAY<JSON>,
    `notavailable` ARRAY<JSON>,
    `availabilityexceptions` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX practitionerrole_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `procedure` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `status` STRING,
    `performeddatetime` STRING,
    `performedperiod_extension` ARRAY<JSON>,
    `performedperiod_start` DATETIME,
    `performedperiod_end` DATETIME,
    `performedstring` STRING,
    `performedage_extension` ARRAY<JSON>,
    `performedage_value` DECIMAL(20,10),
    `performedage_comparator` STRING,
    `performedage_unit` STRING,
    `performedage_system` STRING,
    `performedage_code` STRING,
    `performedrange_extension` ARRAY<JSON>,
    `performedrange_low` JSON,
    `performedrange_high` JSON,
    `performer` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `focaldevice` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX procedure_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "performedstring",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `provenance` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `occurredperiod_extension` ARRAY<JSON>,
    `occurredperiod_start` DATETIME,
    `occurredperiod_end` DATETIME,
    `occurreddatetime` STRING,
    `recorded` STRING,
    `policy` ARRAY<STRING>,
    `agent` ARRAY<JSON>,
    `entity` ARRAY<JSON>,
    `signature_extension` ARRAY<ARRAY<JSON>>,
    `signature_type` ARRAY<ARRAY<JSON>>,
    `signature_when` ARRAY<STRING>,
    `signature_who` ARRAY<JSON>,
    `signature_onbehalfof` ARRAY<JSON>,
    `signature_targetformat` ARRAY<STRING>,
    `signature_sigformat` ARRAY<STRING>,
    `signature_data` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `questionnaire` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `derivedfrom` ARRAY<STRING>,
    `status` STRING,
    `experimental` BOOLEAN,
    `subjecttype` ARRAY<STRING>,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `item` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX questionnaire_status_index (`status`) USING BITMAP,
    INDEX questionnaire_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `questionnaireresponse` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `questionnaire` STRING,
    `status` STRING,
    `authored` DATETIME,
    `item` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX questionnaireresponse_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `relatedperson` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `name_extension` ARRAY<ARRAY<JSON>>,
    `name_use` ARRAY<STRING>,
    `name_text` ARRAY<STRING>,
    `name_family` ARRAY<STRING>,
    `name_given` ARRAY<ARRAY<STRING>>,
    `name_prefix` ARRAY<ARRAY<STRING>>,
    `name_suffix` ARRAY<ARRAY<STRING>>,
    `name_period` ARRAY<JSON>,
    `telecom_extension` ARRAY<ARRAY<JSON>>,
    `telecom_system` ARRAY<STRING>,
    `telecom_value` ARRAY<STRING>,
    `telecom_use` ARRAY<STRING>,
    `telecom_rank` ARRAY<INTEGER>,
    `telecom_period` ARRAY<JSON>,
    `gender` STRING,
    `birthdate` DATETIME,
    `address_extension` ARRAY<ARRAY<JSON>>,
    `address_use` ARRAY<STRING>,
    `address_type` ARRAY<STRING>,
    `address_text` ARRAY<STRING>,
    `address_line` ARRAY<ARRAY<STRING>>,
    `address_city` ARRAY<STRING>,
    `address_district` ARRAY<STRING>,
    `address_state` ARRAY<STRING>,
    `address_postalcode` ARRAY<STRING>,
    `address_country` ARRAY<STRING>,
    `address_period` ARRAY<JSON>,
    `photo_extension` ARRAY<ARRAY<JSON>>,
    `photo_contenttype` ARRAY<STRING>,
    `photo_language` ARRAY<STRING>,
    `photo_data` ARRAY<STRING>,
    `photo_url` ARRAY<STRING>,
    `photo_size` ARRAY<INTEGER>,
    `photo_hash` ARRAY<STRING>,
    `photo_title` ARRAY<STRING>,
    `photo_creation` ARRAY<STRING>,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `communication` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX relatedperson_active_index (`active`) USING BITMAP,
    INDEX relatedperson_gender_index (`gender`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `requestgroup` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `status` STRING,
    `intent` STRING,
    `priority` STRING,
    `authoredon` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `action` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX requestgroup_status_index (`status`) USING BITMAP,
    INDEX requestgroup_intent_index (`intent`) USING BITMAP,
    INDEX requestgroup_priority_index (`priority`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `researchdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `shorttitle` STRING,
    `subtitle` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `comment` ARRAY<STRING>,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `usage` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `library` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX researchdefinition_status_index (`status`) USING BITMAP,
    INDEX researchdefinition_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `researchelementdefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `shorttitle` STRING,
    `subtitle` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `comment` ARRAY<STRING>,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `usage` STRING,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `library` ARRAY<STRING>,
    `type` STRING,
    `variabletype` STRING,
    `characteristic` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX researchelementdefinition_status_index (`status`) USING BITMAP,
    INDEX researchelementdefinition_experimental_index (`experimental`) USING BITMAP,
    INDEX researchelementdefinition_type_index (`type`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, shorttitle, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `researchstudy` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `title` STRING,
    `status` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `description` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `arm` ARRAY<JSON>,
    `objective` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX researchstudy_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "title",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `researchsubject` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `period_extension` ARRAY<JSON>,
    `period_start` DATETIME,
    `period_end` DATETIME,
    `assignedarm` STRING,
    `actualarm` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX researchsubject_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `riskassessment` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `occurrencedatetime` STRING,
    `occurrenceperiod_extension` ARRAY<JSON>,
    `occurrenceperiod_start` DATETIME,
    `occurrenceperiod_end` DATETIME,
    `prediction` ARRAY<JSON>,
    `mitigation` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX riskassessment_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `riskevidencesynthesis` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `copyright` STRING,
    `approvaldate` DATETIME,
    `lastreviewdate` DATETIME,
    `effectiveperiod_extension` ARRAY<JSON>,
    `effectiveperiod_start` DATETIME,
    `effectiveperiod_end` DATETIME,
    `author_extension` ARRAY<ARRAY<JSON>>,
    `author_name` ARRAY<STRING>,
    `author_telecom` ARRAY<ARRAY<JSON>>,
    `editor_extension` ARRAY<ARRAY<JSON>>,
    `editor_name` ARRAY<STRING>,
    `editor_telecom` ARRAY<ARRAY<JSON>>,
    `reviewer_extension` ARRAY<ARRAY<JSON>>,
    `reviewer_name` ARRAY<STRING>,
    `reviewer_telecom` ARRAY<ARRAY<JSON>>,
    `endorser_extension` ARRAY<ARRAY<JSON>>,
    `endorser_name` ARRAY<STRING>,
    `endorser_telecom` ARRAY<ARRAY<JSON>>,
    `relatedartifact_extension` ARRAY<ARRAY<JSON>>,
    `relatedartifact_type` ARRAY<STRING>,
    `relatedartifact_label` ARRAY<STRING>,
    `relatedartifact_display` ARRAY<STRING>,
    `relatedartifact_citation` ARRAY<STRING>,
    `relatedartifact_url` ARRAY<STRING>,
    `relatedartifact_document` ARRAY<JSON>,
    `relatedartifact_resource` ARRAY<STRING>,
    `samplesize_description` STRING,
    `samplesize_numberofstudies` INTEGER,
    `samplesize_numberofparticipants` INTEGER,
    `riskestimate_description` STRING,
    `riskestimate_type` JSON,
    `riskestimate_value` DECIMAL(20,10),
    `riskestimate_unitofmeasure` JSON,
    `riskestimate_denominatorcount` INTEGER,
    `riskestimate_numeratorcount` INTEGER,
    `riskestimate_precisionestimate` ARRAY<JSON>,
    `certainty` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX riskevidencesynthesis_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `schedule` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `active` BOOLEAN,
    `planninghorizon_extension` ARRAY<JSON>,
    `planninghorizon_start` DATETIME,
    `planninghorizon_end` DATETIME,
    `comment` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX schedule_active_index (`active`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `searchparameter` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `derivedfrom` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `code` STRING,
    `base` ARRAY<STRING>,
    `type` STRING,
    `expression` STRING,
    `xpath` STRING,
    `xpathusage` STRING,
    `target` ARRAY<STRING>,
    `multipleor` BOOLEAN,
    `multipleand` BOOLEAN,
    `comparator` ARRAY<STRING>,
    `modifier` ARRAY<STRING>,
    `chain` ARRAY<STRING>,
    `component` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX searchparameter_status_index (`status`) USING BITMAP,
    INDEX searchparameter_experimental_index (`experimental`) USING BITMAP,
    INDEX searchparameter_code_index (`code`) USING BITMAP,
    INDEX searchparameter_type_index (`type`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `servicerequest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` ARRAY<STRING>,
    `instantiatesuri` ARRAY<STRING>,
    `status` STRING,
    `intent` STRING,
    `priority` STRING,
    `donotperform` BOOLEAN,
    `quantityquantity_extension` ARRAY<JSON>,
    `quantityquantity_value` DECIMAL(20,10),
    `quantityquantity_comparator` STRING,
    `quantityquantity_unit` STRING,
    `quantityquantity_system` STRING,
    `quantityquantity_code` STRING,
    `quantityratio_extension` ARRAY<JSON>,
    `quantityratio_numerator` JSON,
    `quantityratio_denominator` JSON,
    `quantityrange_extension` ARRAY<JSON>,
    `quantityrange_low` JSON,
    `quantityrange_high` JSON,
    `occurrencedatetime` STRING,
    `occurrenceperiod_extension` ARRAY<JSON>,
    `occurrenceperiod_start` DATETIME,
    `occurrenceperiod_end` DATETIME,
    `occurrencetiming_extension` ARRAY<JSON>,
    `occurrencetiming_event` ARRAY<STRING>,
    `occurrencetiming_repeat` JSON,
    `occurrencetiming_code` JSON,
    `asneededboolean` BOOLEAN,
    `authoredon` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `patientinstruction` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX servicerequest_status_index (`status`) USING BITMAP,
    INDEX servicerequest_intent_index (`intent`) USING BITMAP,
    INDEX servicerequest_priority_index (`priority`) USING BITMAP,
    INDEX servicerequest_donotperform_index (`donotperform`) USING BITMAP,
    INDEX servicerequest_asneededboolean_index (`asneededboolean`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "patientinstruction",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `slot` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `start` STRING,
    `end` STRING,
    `overbooked` BOOLEAN,
    `comment` STRING,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX slot_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `specimen` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `receivedtime` DATETIME,
    `collection_collector` JSON,
    `collection_collecteddatetime` STRING,
    `collection_collectedperiod` JSON,
    `collection_duration` JSON,
    `collection_quantity` JSON,
    `collection_method` JSON,
    `collection_bodysite` JSON,
    `collection_fastingstatuscodeableconcept` JSON,
    `collection_fastingstatusduration` JSON,
    `processing` ARRAY<JSON>,
    `container` ARRAY<JSON>,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX specimen_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `specimendefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `timeaspect` STRING,
    `typetested` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "timeaspect",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `structuredefinition` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `fhirversion` STRING,
    `mapping` ARRAY<JSON>,
    `kind` STRING,
    `abstract` BOOLEAN,
    `context` ARRAY<JSON>,
    `contextinvariant` ARRAY<STRING>,
    `type` STRING,
    `basedefinition` STRING,
    `derivation` STRING,
    `snapshot_element` ARRAY<JSON>,
    `differential_element` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX structuredefinition_status_index (`status`) USING BITMAP,
    INDEX structuredefinition_experimental_index (`experimental`) USING BITMAP,
    INDEX structuredefinition_fhirversion_index (`fhirversion`) USING BITMAP,
    INDEX structuredefinition_kind_index (`kind`) USING BITMAP,
    INDEX structuredefinition_abstract_index (`abstract`) USING BITMAP,
    INDEX structuredefinition_derivation_index (`derivation`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `structuremap` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `structure` ARRAY<JSON>,
    `import` ARRAY<STRING>,
    `group` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX structuremap_status_index (`status`) USING BITMAP,
    INDEX structuremap_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `subscription` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_system` ARRAY<STRING>,
    `contact_value` ARRAY<STRING>,
    `contact_use` ARRAY<STRING>,
    `contact_rank` ARRAY<INTEGER>,
    `contact_period` ARRAY<JSON>,
    `end` STRING,
    `reason` STRING,
    `criteria` STRING,
    `error` STRING,
    `channel_type` STRING,
    `channel_endpoint` STRING,
    `channel_payload` STRING,
    `channel_header` ARRAY<STRING>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX subscription_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "reason, criteria, error",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `substance` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `description` STRING,
    `instance` ARRAY<JSON>,
    `ingredient` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX substance_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `substancenucleicacid` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `numberofsubunits` INTEGER,
    `areaofhybridisation` STRING,
    `subunit` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "areaofhybridisation",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `substancepolymer` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `modification` ARRAY<STRING>,
    `monomerset` ARRAY<JSON>,
    `repeat` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `substanceprotein` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `numberofsubunits` INTEGER,
    `disulfidelinkage` ARRAY<STRING>,
    `subunit` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `substancereferenceinformation` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `comment` STRING,
    `gene` ARRAY<JSON>,
    `geneelement` ARRAY<JSON>,
    `classification` ARRAY<JSON>,
    `target` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "comment",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `substancesourcematerial` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `organismname` STRING,
    `parentsubstancename` ARRAY<STRING>,
    `geographicallocation` ARRAY<STRING>,
    `fractiondescription` ARRAY<JSON>,
    `organism_family` JSON,
    `organism_genus` JSON,
    `organism_species` JSON,
    `organism_intraspecifictype` JSON,
    `organism_intraspecificdescription` STRING,
    `organism_author` ARRAY<JSON>,
    `organism_hybrid` JSON,
    `organism_organismgeneral` JSON,
    `partdescription` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "organismname",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `substancespecification` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `description` STRING,
    `comment` STRING,
    `moiety` ARRAY<JSON>,
    `property` ARRAY<JSON>,
    `structure_stereochemistry` JSON,
    `structure_opticalactivity` JSON,
    `structure_molecularformula` STRING,
    `structure_molecularformulabymoiety` STRING,
    `structure_isotope` ARRAY<JSON>,
    `structure_molecularweight` JSON,
    `structure_source` JSON,
    `structure_representation` ARRAY<JSON>,
    `code` ARRAY<JSON>,
    `name` ARRAY<JSON>,
    `molecularweight` ARRAY<JSON>,
    `relationship` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description, comment",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `supplydelivery` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `supplieditem_quantity` JSON,
    `supplieditem_itemcodeableconcept` JSON,
    `supplieditem_itemreference` JSON,
    `occurrencedatetime` STRING,
    `occurrenceperiod_extension` ARRAY<JSON>,
    `occurrenceperiod_start` DATETIME,
    `occurrenceperiod_end` DATETIME,
    `occurrencetiming_extension` ARRAY<JSON>,
    `occurrencetiming_event` ARRAY<STRING>,
    `occurrencetiming_repeat` JSON,
    `occurrencetiming_code` JSON,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX supplydelivery_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `supplyrequest` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `priority` STRING,
    `quantity_extension` ARRAY<JSON>,
    `quantity_value` DECIMAL(20,10),
    `quantity_comparator` STRING,
    `quantity_unit` STRING,
    `quantity_system` STRING,
    `quantity_code` STRING,
    `parameter` ARRAY<JSON>,
    `occurrencedatetime` STRING,
    `occurrenceperiod_extension` ARRAY<JSON>,
    `occurrenceperiod_start` DATETIME,
    `occurrenceperiod_end` DATETIME,
    `occurrencetiming_extension` ARRAY<JSON>,
    `occurrencetiming_event` ARRAY<STRING>,
    `occurrencetiming_repeat` JSON,
    `occurrencetiming_code` JSON,
    `authoredon` DATETIME,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX supplyrequest_status_index (`status`) USING BITMAP,
    INDEX supplyrequest_priority_index (`priority`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `task` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `instantiatescanonical` STRING,
    `instantiatesuri` STRING,
    `status` STRING,
    `intent` STRING,
    `priority` STRING,
    `description` STRING,
    `executionperiod_extension` ARRAY<JSON>,
    `executionperiod_start` DATETIME,
    `executionperiod_end` DATETIME,
    `authoredon` DATETIME,
    `lastmodified` DATETIME,
    `note_extension` ARRAY<ARRAY<JSON>>,
    `note_authorreference` ARRAY<JSON>,
    `note_authorstring` ARRAY<STRING>,
    `note_time` ARRAY<STRING>,
    `note_text` ARRAY<STRING>,
    `restriction_repetitions` INTEGER,
    `restriction_period` JSON,
    `restriction_recipient` JSON,
    `input` ARRAY<JSON>,
    `output` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX task_status_index (`status`) USING BITMAP,
    INDEX task_intent_index (`intent`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "description",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `terminologycapabilities` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `kind` STRING,
    `software_name` STRING,
    `software_version` STRING,
    `implementation_description` STRING,
    `implementation_url` STRING,
    `lockeddate` BOOLEAN,
    `codesystem` ARRAY<JSON>,
    `expansion_hierarchical` BOOLEAN,
    `expansion_paging` BOOLEAN,
    `expansion_incomplete` BOOLEAN,
    `expansion_parameter` ARRAY<JSON>,
    `expansion_textfilter` STRING,
    `codesearch` STRING,
    `validatecode_translations` BOOLEAN,
    `translation_needsmap` BOOLEAN,
    `closure_translation` BOOLEAN,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX terminologycapabilities_status_index (`status`) USING BITMAP,
    INDEX terminologycapabilities_experimental_index (`experimental`) USING BITMAP,
    INDEX terminologycapabilities_kind_index (`kind`) USING BITMAP,
    INDEX terminologycapabilities_lockeddate_index (`lockeddate`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `testreport` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `name` STRING,
    `status` STRING,
    `result` STRING,
    `score` DECIMAL(20,10),
    `tester` STRING,
    `issued` DATETIME,
    `participant` ARRAY<JSON>,
    `setup_action` ARRAY<JSON>,
    `test` ARRAY<JSON>,
    `teardown_action` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX testreport_status_index (`status`) USING BITMAP,
    INDEX testreport_result_index (`result`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "name, tester",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `testscript` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `purpose` STRING,
    `copyright` STRING,
    `origin` ARRAY<JSON>,
    `destination` ARRAY<JSON>,
    `metadata_link` ARRAY<JSON>,
    `metadata_capability` ARRAY<JSON>,
    `fixture` ARRAY<JSON>,
    `variable` ARRAY<JSON>,
    `setup_action` ARRAY<JSON>,
    `test` ARRAY<JSON>,
    `teardown_action` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX testscript_status_index (`status`) USING BITMAP,
    INDEX testscript_experimental_index (`experimental`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `valueset` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `url` STRING,
    `version` STRING,
    `name` STRING,
    `title` STRING,
    `status` STRING,
    `experimental` BOOLEAN,
    `date` DATETIME,
    `publisher` STRING,
    `contact_extension` ARRAY<ARRAY<JSON>>,
    `contact_name` ARRAY<STRING>,
    `contact_telecom` ARRAY<ARRAY<JSON>>,
    `description` STRING,
    `usecontext_extension` ARRAY<ARRAY<JSON>>,
    `usecontext_code` ARRAY<JSON>,
    `usecontext_valuecodeableconcept` ARRAY<JSON>,
    `usecontext_valuequantity` ARRAY<JSON>,
    `usecontext_valuerange` ARRAY<JSON>,
    `usecontext_valuereference` ARRAY<JSON>,
    `immutable` BOOLEAN,
    `purpose` STRING,
    `copyright` STRING,
    `compose_lockeddate` DATETIME,
    `compose_inactive` BOOLEAN,
    `compose_include` ARRAY<JSON>,
    `compose_exclude` ARRAY<JSON>,
    `expansion_identifier` STRING,
    `expansion_timestamp` DATETIME,
    `expansion_total` INTEGER,
    `expansion_offset` INTEGER,
    `expansion_parameter` ARRAY<JSON>,
    `expansion_contains` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX valueset_status_index (`status`) USING BITMAP,
    INDEX valueset_experimental_index (`experimental`) USING BITMAP,
    INDEX valueset_immutable_index (`immutable`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"bloom_filter_columns" = "version, name, title, publisher",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `verificationresult` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `targetlocation` ARRAY<STRING>,
    `status` STRING,
    `statusdate` DATETIME,
    `frequency_extension` ARRAY<JSON>,
    `frequency_event` ARRAY<STRING>,
    `frequency_repeat` JSON,
    `frequency_code` JSON,
    `lastperformed` DATETIME,
    `nextscheduled` DATETIME,
    `primarysource` ARRAY<JSON>,
    `attestation_who` JSON,
    `attestation_onbehalfof` JSON,
    `attestation_communicationmethod` JSON,
    `attestation_date` DATETIME,
    `attestation_sourceidentitycertificate` STRING,
    `attestation_proxyidentitycertificate` STRING,
    `attestation_proxysignature` JSON,
    `attestation_sourcesignature` JSON,
    `validator` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX verificationresult_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

CREATE TABLE IF NOT EXISTS `visionprescription` (
    `id` VARCHAR(100),
    `meta_extension` ARRAY<JSON>,
    `meta_versionid` VARCHAR(100),
    `meta_lastupdated` STRING,
    `meta_source` STRING,
    `meta_profile` ARRAY<STRING>,
    `meta_security` ARRAY<JSON>,
    `meta_tag` ARRAY<JSON>,
    `language` STRING,
    `text_extension` ARRAY<JSON>,
    `text_status` STRING,
    `text_div` STRING,
    `extension` ARRAY<JSON>,
    `status` STRING,
    `created` DATETIME,
    `datewritten` DATETIME,
    `lensspecification` ARRAY<JSON>,
    `updated_date` DATETIME NOT NULL default current_timestamp,
    `codeableconcept_max_array_size` INT,
    `reference_max_array_size` INT,
    `identifier_max_array_size` INT,
    INDEX visionprescription_status_index (`status`) USING BITMAP
)ENGINE=olap
PRIMARY KEY (id)
DISTRIBUTED BY HASH(id) BUCKETS 16
PROPERTIES
(
"storage_type"="column",
"compression" = "LZ4",
"enable_async_write_back" = "false",
"enable_persistent_index" = "true",
"persistent_index_type" = "CLOUD_NATIVE",
"replication_num"= "1",
"colocate_with" = "core_group"
);

